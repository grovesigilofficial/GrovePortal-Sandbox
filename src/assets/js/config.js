// src/assets/js/config.js
// Local-only config for the Sandbox.
// Keep this file in each repo (Dev/Main can reuse later).
export const CONFIG = {
  adminUsername: "Bleedvow",
  adminPassword: "ES@2610011171",
  // localStorage key used for the counter
  LOCAL_COUNTER_KEY: "grove_uberman_day"
};
